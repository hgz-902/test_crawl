# Agent Policy

이 저장소에서 작업할 때는 아래 원칙을 우선한다.

## 공통 원칙

- 크롤러 소스는 모든 사이트에 공통으로 적용되는 동작만 포함한다.
- 사이트별 동작 차이는 가능한 한 `configs/*.json`에 둔다.
- 공통 크롤러 소스는 특정 사이트 전용 로직으로 오염시키지 않는다.
- 예외가 필요한 경우에는 먼저 config 변경으로 해결 가능한지 확인한다.
- config로 해결되지 않는 경우에만 실행 단계에서 예외 처리나 분기 추가를 검토한다.

## 금지 방향

- 사이트별 전용 크롤러를 소스에 추가하지 않는다.
- 특정 사이트만 위한 하드코딩 selector, URL 분기, DOM 예외 처리를 공통 코드에 넣지 않는다.
- 공통 코드 수정은 여러 사이트에 일반적으로 필요한 경우에만 한다.

## 판단 기준

- 공통화 가능하면 공통 크롤러를 수정한다.
- 사이트 고유 특성이면 해당 `config`를 수정한다.
- 공통화 여부가 불명확하면 우선 config 기준으로 해결 가능한지 검토한다.

## 작업 우선순위

1. `configs/*.json` 수정 가능성 확인
2. 실행 단계에서 처리 가능한지 확인
3. 그 다음에만 공통 크롤러 소스 수정 검토

