# 20260701 DB 변경 후 작업

## 변경 내용

- `crawl_articles` 테이블에 `source_config_category` 컬럼이 추가된다.
- 신규/재동기화 기사 적재 시 `configs/*.json`의 `category` 값이 `source_config_category`에 저장된다.
- `/api/news`, `/api/news/grouped`, `/api/stats`, `/api/filter-options`는 기본적으로 `config_category=PR` 기준 데이터를 반환한다.
- `category: "GR,PR"`처럼 쉼표로 여러 category가 등록된 config는 토큰 중 하나라도 매칭되면 포함된다.

## 배포 후 필수 작업

1. 최신 코드 배포 후 서버를 재시작한다.
2. 뉴스 UI DB를 사용하는 환경에서 아래 동기화를 1회 실행한다.

```powershell
python -m crawler_app.news_ingestion --project-root D:\CompPjts\crawlNew --progress
```

3. 기존 DB를 완전히 다시 만들고 싶을 때만 `--rebuild`를 붙인다.

```powershell
python -m crawler_app.news_ingestion --project-root D:\CompPjts\crawlNew --rebuild --progress
```

`--progress`를 붙이면 파일 읽기, category backfill, 감성분석, 날짜별 그룹핑 진행 상황이 콘솔에 출력된다. 진행 로그가 필요 없으면 `--progress`를 생략해도 된다.

## 확인 SQL

```sql
PRAGMA table_info(crawl_articles);
```

`source_config_category` 컬럼이 있어야 한다.

```sql
SELECT source_config_name, source_config_category, COUNT(*) AS cnt
FROM crawl_articles
GROUP BY source_config_name, source_config_category
ORDER BY cnt DESC;
```

`source_config_category`에 `PR`, `GR`, `GR,PR`, `CR` 같은 값이 채워졌는지 확인한다.

## API 확인

파라미터를 생략하면 기본적으로 `config_category=PR`로 동작한다.

```http
GET http://127.0.0.1:3010/api/news?user_id=unknown&page=1&page_size=20
```

전체 category 제한을 해제하려면:

```http
GET http://127.0.0.1:3010/api/news?user_id=unknown&config_category=all&page=1&page_size=20
```

PR과 GR을 같이 보려면:

```http
GET http://127.0.0.1:3010/api/news?user_id=unknown&config_category=PR,GR&page=1&page_size=20
```

## 주의 사항

- DB에 `source_config_category`가 비어 있으면 기본 `config_category=PR` 조회에서 해당 기사는 제외된다.
- 기존 데이터는 동기화 실행 시 `source_config_name`과 현재 `configs/*.json`의 `name`을 기준으로 category가 보강된다.
- config의 `name`이 바뀌면 기존 DB의 `source_config_name`과 매칭되지 않을 수 있다.
- `category_code=SK`는 회사/키워드 분류이고, `config_category=PR`은 JSON config 분류다. 두 값은 서로 다른 필터다.
