# 중복 검색어 배열화 동기화 확인

- 일시: 2026-06-24 18:13 KST
- 대상 저장소: `hgz-902/test_crawl`
- 대상 브랜치: `dev/crawl-20260624`
- 작업 워크스페이스: `C:\AI_JOB\firstproject\crawler_project\crawler-tran-flat-test`
- push용 repo: `C:\AI_JOB\firstproject\crawler_project\test_crawl`

## 목적

`crawler-tran-flat-test`에 반영된 최신 crawler 코드와 설정 상태가 `hgz-902/test_crawl`의 `dev/crawl-20260624` 브랜치와 동일한지 확인하고, 이번 중복 검색어/필터어 배열화 검수 및 배포 요청을 push-history로 남긴다.

## 상태

`crawler-tran-flat-test`와 `test_crawl`의 주요 소스/설정/테스트 경로를 해시 비교한 결과, 차이가 없었다.

비교 범위:

- `crawler_app`
- `tests`
- `templates`
- `static`
- `configs`
- `scripts`
- `requirements.txt`

따라서 이 저장소에서는 코드 변경 없이 push-history만 추가한다.

## 현재 포함된 주요 기능

- Parquet flat 변환 구조와 `/parquet-converter` UI
- SQLite 기반 뉴스 UI API
- 사전 그룹핑/감성분석 저장 구조
- `workflow_records.json`의 `search_term`, `filter_term` 배열화
- 중복 진단 시 대표 record로 term 배열 병합
- 배열형 term에 대한 workflow records API 및 SQLite 적재 호환

## 검증

이 브랜치는 직전 push 시 다음 검증을 통과한 코드와 동일한 상태다.

- `compileall crawler_app`
- workflow/news UI/workflow records 관련 unittest

이번 요청에서는 `crawler-tran-flat-test`와 `test_crawl` 간 주요 파일 해시 비교 결과 `COUNT 0`으로 확인했다.

## 비밀값 점검

- `.env`, API key, token, cookie, 비밀번호 파일은 포함하지 않는다.
- `outputs`, `runtime`, `logs`, `orchestration_state`, `.venv`, `qa-artifacts`는 push 대상에서 제외한다.

## 남은 리스크

- 실제 운영 환경에서 `search_term`이 페이지 파라미터로 쓰이는 설정은 배열화된 값의 의미가 검색어가 아니라 페이지 경계 중복일 수 있다.
- 장기적으로 `search_term_role` 같은 의미 구분 필드를 도입하면 운영자가 결과를 더 명확히 해석할 수 있다.
